<%= form_for(@pin, :html => { :multipart => true }) do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="field">
    <%= f.label :path %><br>
    <%= f.file_field :path %>
  </div>  
  <div class="field">
    <%= f.label :type_id %><br>
    <%= select_tag "pin[type_id]", options_from_collection_for_select(@type, "id", "name", @pin.type_id), prompt: "Select something" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
